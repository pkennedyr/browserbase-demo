# Required. Launches browsers and pays for Model Gateway calls (Stagehand) on the same key.
BROWSERBASE_API_KEY="YOUR_BROWSERBASE_API_KEY"
# Optional. The current API infers the project from the key; set it only to be explicit.
BROWSERBASE_PROJECT_ID=""
